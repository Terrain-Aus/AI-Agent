{
  "name": "@terrainpro/apprentice-service",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "TerrainPro Apprentice — standalone guardian service. M4A adds a framework-free POST /review transport adapter around the deterministic review contract (no server/listener, no Cloud Run, no Firestore, no AI runtime wiring). Advisory-only: returns RemediationFlag[] and never mutates anything.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@google/genai": "^2.10.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
